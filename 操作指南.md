# Superposition CAP 操作指南

本指南面向实际使用 GUI 与硬件（AWG520 + Keysight 示波器）进行功率域叠加 16QAM 实验的用户。

---

## 1. 快速启动

### 1.1 环境准备

```bash
python -m venv .venv
.venv\Scripts\activate.bat
pip install -r requirements.txt
```

确认已安装：
- NI-VISA / Keysight IO Libraries（示波器 TCP/IP 需要）
- Tektronix VISA / NI-VISA（AWG520 GPIB 需要）

### 1.2 启动 GUI

```bash
python superposition_gui.py
```

程序默认打开 5 个页签：
- **波形与频谱**：查看发射/接收波形、频谱、星座图
- **叠加调制**：设置 PAM4 映射、子载波、roll-off
- **传输实验结果**：历史记录表格，含 BER、实际 SNR、传输速率
- **运行测试**：选择数据源、运行实验、AWG 控制、LMS 优化
- **示波器**：手动连接示波器并采集

---

## 2. 运行模式

在“运行测试”页选择数据源：

| 数据源 | 用途 | 说明 |
|---|---|---|
| `virtual` | 离线仿真 | 使用内置 VLC 虚拟信道 + AWGN，无需硬件 |
| `file` | 离线处理已存波形 | 读取 `data/rxdata/rx_<id>.txt`，用当前 LMS 参数重新解调 |
| `scope` | 在线采集 | 通过 GPIB/LAN 控制 AWG 发射，示波器采集后实时处理 |

### 2.1 virtual 模式

1. 设置 **SNR (dB)**、符号数、随机种子
2. 设置 LMS 参数（taps、μ1、μ2）
3. 点击 **运行仿真**
4. 结果自动保存到 `data/records/record_<run_id>.json`

### 2.2 file 模式

1. 在“运行测试”页选择 `file`
2. 程序会自动列出 `data/rxdata/` 中的接收文件
3. 点击 **运行仿真**
4. 默认启用 LMS 参数优化时，会先扫描最优 taps/μ，再用最优参数重新解调
5. 结果复用原 `run_id`；如果 BER 比旧记录差，会保留旧记录并弹窗提示

### 2.3 scope 模式

1. 确认 AWG520 已开机并通过 GPIB 连接
2. 确认示波器 IP/GPIB 地址正确（默认 `TCPIP0::169.254.140.83::5025::SOCKET`）
3. 设置符号数、种子、AWG 采样率、Vpp
4. 点击 **下载到 AWG**，再点击 **开始输出**
5. 点击 **运行仿真**，程序会：
   - 生成发射波形
   - 下载到 AWG 双通道
   - 启动 AWG 输出
   - 触发示波器采集
   - 做同步、均衡、BER 计算
6. 实际 SNR 会从 LMS 均衡后的符号估计得到

---

## 3. AWG 控制

“运行测试”页 AWG 卡片：

| 按钮 | 作用 |
|---|---|
| **下载到 AWG** | 生成当前参数下的波形并下载到 CH1/CH2 |
| **开始输出** | 启动 AWG 输出（仅加载波形的通道） |
| **停止输出** | 停止 AWG 输出 |
| **应用输出设置** | 单独更新采样率、Vpp，不重下载波形 |
| **清空 AWG** | 清空 AWG 内存波形并停止输出 |
| **仅生成波形** | 只生成 `txI_*.txt`、`txQ_*.txt` 和 `.wfm`，不连接 AWG |

注意：
- 修改调制格式或叠加方式后，必须重新下载波形，否则 AWG 会播放旧波形
- Vpp 或采样率变更后，点击 **应用输出设置** 即可同步到 AWG

---

## 4. 单通道叠加模式

在“运行测试”页勾选 **叠加到单通道** 后：

- CH1 与 CH2 的基带波形会相加后只从 CH1 输出
- 示波器只需采集一个端口（默认 CHAN4）
- 接收端仍按 2x2 MIMO LMS 处理，分离 I/Q 两路

注意：这是两路**实数波形相加**，不是复数叠加；叠加后的信号仍为实数。

---

## 5. LMS 参数自动优化

“运行测试”页勾选 **启用 LMS 参数扫参** 后，file/scope 模式会执行坐标下降优化：

1. 第一轮：全范围扫描 taps → μ1 → μ2
2. 第二轮：在已收敛 μ 上重新选择 taps
3. 第三轮：在第二轮最优值附近 1/3 ~ 3 倍 log 范围细化 μ

优化完成后：
- 最优参数自动写回当前 LMS 设置
- 用最优参数重新运行一次实验
- 完整扫描结果保存到 `data/records/sweep_<run_id>.csv`

如需手动设置参数，取消勾选即可。

---

## 6. 结果查看

### 6.1 历史记录表格

“传输实验结果”页显示所有实验：
- Run ID
- 时间
- 调制格式
- 数据源
- 符号数
- 实际 SNR (dB)
- 平均 BER
- 传输速率 (Mbps)
- 备注

双击某行可重新加载并显示对应波形/星座图。

### 6.2 文件位置

```
data/
├── txdata/          # 发射波形 txI_*.txt, txQ_*.txt, *.wfm
├── rxdata/          # 接收波形 rx_*.txt, eq_*.txt
├── records/         # 实验记录 record_*.json, record_*.txt, sweep_*.csv
└── plots/           # 图像
```

---

## 7. 常见问题

### 7.1 找不到示波器

- 检查示波器 IP/GPIB 地址
- 确认示波器网络接口已启用 SCPI Socket
- 在“示波器”页手动输入地址后点击连接

### 7.2 AWG 点击开始输出无反应

- 确认已先点击 **下载到 AWG**
- 检查 GPIB 连接和地址 `GPIB0::1::INSTR`
- 查看程序日志是否有 VISA 错误

### 7.3 CH2 信号消失 / BER 异常

- 检查是否勾选了 **叠加到单通道**；勾选后只有 CH1 输出
- 检查调制格式变更后是否重新下载波形
- 检查 AWG Vpp 是否同步到硬件

### 7.4 file 模式 shape 广播错误

- 确保选择的接收文件是 `rx_<run_id>.txt`（原始接收波形）
- 不要选择 `eq_*.txt`（均衡后符号），程序已屏蔽这类文件

### 7.5 误码率 0.5

- 通常意味着波形错误或同步失败
- 检查 AWG 是否正在播放正确波形
- 检查示波器采集通道和采样率
- 尝试重新生成并下载波形

---

## 8. 命令行入口

离线快速运行：

```bash
python main.py --data-source virtual --snr 20
```

批量重跑历史 file 记录并更新 SNR/BER：

```bash
python batch_reprocess.py
```

清理无对应波形文件的空记录：

```bash
python cleanup_records.py
```
